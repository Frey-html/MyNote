#### 学习规划
- [ ] Game101图形学


#### 生活规划

```python
   class Node:
        nodeColor = 'X'
        expendedChildList = []
        isFullyExpended = False
        totalScore = 0
        visitTimes = 0
        ucb = float("inf")
        
        
        def __init__(self, action, parent, color):
            self.action = action   #parent's action that result in this node 
            self.parent = parent
            nodeColor = color
            
        def setChildList(childList):
            self.childList = childList
        
        def visit():
            self.visitTimes += 1
            
            
    class MCST:
        
```